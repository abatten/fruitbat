language: python
os: linux
dist: xenial
sudo: false

python:
    - "3.6"
    - "3.7"
    - "3.8"
    - "3.9"

install:
    - pip install --upgrade pip setuptools
    - pip install -r requirements_dev.txt

script:
    - pytest

after_success:
    - codecov
