language: python
os: linux
dist: xenial
sudo: false

python:
    - "2.7"
    - "3.5"
    - "3.6"
    - "3.7"

install:
    - pip install pytest-cov
    - pip install codecov
    - pip install -r requirements.txt

script:
    - pytest

after_success:
    - codecov
