language: python
os: linux
dist: xenial
sudo: false

python:
    - "3.5"
    - "3.6"
    - "3.7"

install:
    - pip install tox
    - pip install codecov
    - pip install -r requirements.txt

script:
    - tox -v -e py

after_success:
    - codecov
